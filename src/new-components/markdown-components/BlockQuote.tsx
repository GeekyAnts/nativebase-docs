import React from "react";
import { Heading, Text, useColorModeValue } from "native-base";
import { getHeadingIds } from "../../utils";

export const BlockQuote = ({ children }: any) => {
  return <Text></Text>;
};
